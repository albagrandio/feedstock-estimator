# Unpinned + minimal so it resolves on current Python (incl. 3.14) on Streamlit Cloud.
# Only what the app imports; DB libs (sqlalchemy/psycopg2/connectorx) are kept so the
# Spain NR path can import, but it won't connect from Streamlit Cloud (private DB).
streamlit
streamlit-folium
folium
branca
geopandas
shapely
pyproj
pandas
numpy
polars
plotly
reportlab
pillow
pyyaml
requests
python-dotenv
xlsxwriter
sqlalchemy
psycopg2-binary
connectorx
